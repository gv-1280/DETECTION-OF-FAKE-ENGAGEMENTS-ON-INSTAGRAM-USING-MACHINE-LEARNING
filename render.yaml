services:
  - type: web
    name: fake-engagement-detector
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run src/app.py --server.port $PORT
    envVars:
      - key: PORT
        value: 10000
